{
  "name": "events-coreviaflow",
  "version": "1.0.0",
  "description": "Meta Conversions API server-side proxy. Receives events from landing/CRM/Monobank, forwards to Meta with hashed PII and dedup support.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^4.19.2"
  }
}
